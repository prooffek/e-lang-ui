export enum NavigationParams {
  collectionId = 'collectionId',
  editCollectionId = 'editCollectionId',
  editFlashcardId = 'editFlashcardId',
  form = 'form',
  attempts = 'attempts',
  attempt = 'attempt',
}

export enum ViewParams {
  main = 'main',
  collection = 'collection',
}

export enum FormTypeParams {
  add = 'add',
  edit = 'edit',
}

export enum Columns {
  singleColumn = '1',
  doubleColumn = '2',
}
